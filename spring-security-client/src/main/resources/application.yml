server:
    port: 8082
spring:
    datasource:
        username: postgres
        password: mwas
        url: jdbc:postgresql://localhost:5430/user_registration
    jpa:
        hibernate:
            ddl-auto: update
        properties:
            hibernate:
                dialect: org.hibernate.dialect.PostgreSQLDialect
                format_sql: true
        show-sql: true
